version: "3.9"

# Default: golink with SQLite (data stored in ./data)
services:
  golink:
    image: ghcr.io/mkende/golink-redirector:latest
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    environment:
      - TZ=UTC
